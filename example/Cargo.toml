[package]
name = "hello"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = 'fat'
codegen-units = 1
opt-level = 'z'
strip = true
debug = true


[dependencies]
fast-stream = { git = "https://github.com/dounine/fast-stream", features = ["all"] }
iospng = { path = "..", features = ["all"] }
