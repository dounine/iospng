[package]
name = "example"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = 'fat'
codegen-units = 1
opt-level = 'z'
strip = true
debug = false


[dependencies]
iospng = { path = ".." }
fast_stream = { path = "/Users/lake/dounine/github/ipa/fast-stream", features = ["enum"] }
