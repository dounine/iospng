[package]
name = "iospng"
version = "0.1.0"
edition = "2021"

exclude = ["example"]
[workspace]
members = [".", "example"]

[dependencies]
thiserror = "2.0.9"
fast-stream = { path = "../fast-stream", features = ["file", "crc32", "enum", "deflate", "enum"] }