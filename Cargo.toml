[package]
name = "iospng"
version = "0.1.0"
edition = "2024"

exclude = ["example"]
[workspace]
members = [".", "example"]

[features]
default = ["fast-stream/mem"]
all = ["fast-stream/all"]

[dependencies.fast-stream]
optional = true
path = "../fast-stream"

[dependencies]
thiserror = "2.0.9"